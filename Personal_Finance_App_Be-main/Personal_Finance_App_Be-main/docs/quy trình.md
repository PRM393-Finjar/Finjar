# Kéo code

1. git branch: kiểm tra xem mình đang ở branch nào
2. git fetch : so sánh code giữa local vs lại github
3. git pull : kéo code mới nhất về

# Đẩy code

    1. git branch < tên branch>
    2. git checkout < tên branch>
    3. code cho đã
    4. git add .
    5. git commit-m"commit"
    6. git push origin < tên branch>
